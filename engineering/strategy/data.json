{
  "title": "Strategy",
  "description": "The Strategy design pattern defines a family of algorithms, encapsulates each one and makes them interchangeable, allowing the algorithm to be selected at runtime.",
  "categories": [
    "design pattern"
  ],
  "content": "The design pattern that defines a set of algorithms and encapsulates them in separate classes. It allows the selection of the appropriate algorithm at runtime, making the system more flexible and easier to extend.",
  "extensions": {}
}