{
  "title": "useInsertionEffect()",
  "description": "The `useInsertionEffect()` hook is used to perform operations before the DOM is painted.",
  "categories": [
    "hook"
  ],
  "content": "The hook that runs synchronously after DOM mutations but before the browser has painted. It’s useful for injecting styles or making changes that need to be applied immediately to avoid visual glitches.",
  "extensions": {}
}