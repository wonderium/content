{
  "title": "useRef()",
  "description": "The `useRef()` hook returns a mutable ref object whose `.current` property can be used to hold a DOM element or a value that persists across renders without causing re-renders.",
  "categories": [
    "hook"
  ],
  "content": "The hook that provides a way to access and interact with DOM elements or store mutable values that persist across renders. It’s commonly used for accessing elements directly or keeping a reference to a value that doesn’t trigger re-renders.",
  "extensions": {}
}