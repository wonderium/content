{
  "title": "renderToStaticMarkup()",
  "description": "The `renderToStaticMarkup()` API renders a React component to static HTML markup, excluding React-specific attributes like `data-reactroot`.",
  "categories": [
    "API"
  ],
  "content": "The function that renders a React component to static HTML markup without adding extra React-specific attributes. This is useful for generating static HTML content that doesn't require React's functionality on the client side.",
  "extensions": {}
}