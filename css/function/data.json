{
  "title": "Function",
  "description": "The Function in CSS, such as `calc()` or `rgba()`, performs calculations or manipulations on values.",
  "categories": [
    "concept",
    "syntax"
  ],
  "content": "The special commands that take one or more arguments and return a value used in a style rule. Examples include `calc()` for calculations, `url()` for linking resources and `rgb()` for color definitions. Functions provide dynamic and flexible ways to define styles.",
  "extensions": {}
}